# 聊天与文件引用

**Chat** 按钮位于应用顶部栏右上角。

## 选择 Agent 与模型

聊天框左侧依次是 **Agent**、**Model**、**Mode** 三个选择器。**Agent** 决定
对话的后端（Nest Agent 或 Claude），**Model** 显示将要使用的模型，**Mode**
即下文的 **Ask** / **Agent** 模式。完整的说明 —— Agent、绑定、模型与配置 ——
见[《Agents》](./Agents.md)指南。

## 在已启用知识中提问

没有引用的提示词会搜索所有已启用知识包。请说明想要的结果、受众和格式。

## 使用 `@` 聚焦

输入 `@`，选择文件或文件夹，即可将检索范围限定在该引用上，可组合多个引用。

```text
@getting-started-zh-cn/guides/编辑与源代码管理.md
请给我一份发布前审查修改的检查清单。
```

## 检查引用来源

回答不完整或范围过大时，打开引用核对来源，再重新描述问题或缩小 `@` 引用范围。

## 使用建议

1. 只启用相关知识包。
2. 每次提出一个明确任务。
3. 说明受众与输出格式。
4. 根据引用核对重要内容。
